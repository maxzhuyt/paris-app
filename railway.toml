[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --timeout 600 --workers 1 --threads 1 --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
